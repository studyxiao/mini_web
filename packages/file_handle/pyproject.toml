[project]
name = "file-handle"
version = "0.1.0"
authors = [
    { name = "studyxiao", email = "studyxiao@163.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
